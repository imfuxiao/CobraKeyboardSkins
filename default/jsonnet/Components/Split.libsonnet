// 分体键盘（Split）：键盘从中间分成左右两半，两个大拇指各管一边。
// 机制见 docs/键盘Split状态.md，这里只放「极简」这套皮肤的分体版面。
//
// ===== 三条约束决定了下面这些写法 =====
//
// 1. **不新增布局文件，也不改布局树的形状。** 分体与合并读的是同一棵
//    `keyboardLayout`，只有各节点的 `split` 覆盖块不同。所以分体态才用得上的键
//    （中缝、两侧留白、右半边那颗空格）必须**平时就在树上**，宽度写 0；
//    宽或高不足一个物理像素的键不建图层，合并态下它们逐像素不存在。
//
// 2. **不改键盘总高度。** 总高挂在根节点上，`split` 够不着。分体态靠把第一行
//    （数字行）的高度压成 0 腾出地方，剩下四行分掉整块高度，键会比合并态高一截。
//
// 3. **每行的宽度必须自平。** 一行里所有键的分体宽度加起来要正好是一整行，
//    多了少了都没人拦。下面每张表后面都记了账，改宽度时连账一起改。
//
// ===== 进出分体只靠一个上划手势，没有专门的「把手」键 =====
//
// `#toggleSplitState` 是个**翻转**指令：同一颗键上划一次进分体，态在分体下再上划
// 一次就合回去——因为那颗键的 `swipeUpAction` 不放在 `split` 覆盖块里，是写在节点
// 本层的（`enterSplitGesture`），分体后这个手势原样保留，不会被覆盖掉。
// 于是合并 / 分体两态共用同一个手势，不需要另开一颗「点一下合回去」的按钮。
local Style = import 'Style.libsonnet';

// ===== iPad 分体态的宽度表（分母 16）=====
//
// 左右两半关于键盘中线（8.0）对称，中缝宽度逐行不同但缝心都落在 8.0 上：
// 第二行两侧各留 0.9，第三行缩进半颗键（1.4），第四行留 0.9 但多一颗 Shift。
// 这就是键盘该有的错位——a 比 q 靠右半颗，z 又比 a 靠右半颗。
//
//   第二行  0.9 + 5 + 4.2 + 5 + 0.9              = 16
//   第三行  1.4 + 5 + 3.2 + 5 + 1.4              = 16
//   第四行  0.9 + 1.5 + 4 + 3.2 + 4 + 1.5 + 0.9  = 16
//   第五行  1.5 + 1.9 + 2.5 + 4.2 + 2.5 + 1.9 + 1.5 = 16
local w = {
  unit: '1/16',  // 分体态的一颗普通键，比合并态（1.1）窄一点
  pad: '0.9/16',  // 第二 / 四行的外侧留白
  padHome: '1.4/16',  // 第三行的外侧留白（多缩进半颗键）
  gapTop: '4.2/16',  // 第二行的中缝
  gapHome: '3.2/16',  // 第三行的中缝
  gapBottom: '3.2/16',  // 第四行的中缝
  gapSpace: '4.2/16',  // 第五行的中缝
  tab: '0.9/16',  // Tab 键分体后的宽度（上划进 / 出分体，见文件开头的说明）
  shift: '1.5/16',
  backspace: '1.5/16',
  globe: '1.5/16',
  keyboardType: '1.9/16',  // 123 / 拼音
  space: '2.5/16',  // 左右各一颗
  enter: '1.9/16',
  dismiss: '1.5/16',
};

// ===== iPhone 横屏分体态的宽度表（分母 1125，与 Widths.iPhone 同一套分母）=====
//
// 四行共用**同一条外侧留白 `margin`**：q、Shift、123（或 #+=）三颗键左侧对齐，
// 右边 p、删除、回车三颗键也右侧对齐，第二行（asdf）也不例外。
//
// **第一、四行共用同一条中缝 `gap`**：两行左右两侧「内容宽度」算出来正好相等
// （都是 5 个 `unit`），边界精确重合——t 的右边缘正好是左半空格的右边缘，
// y 的左边缘正好是右半空格的左边缘。
//
// **第三行（Shift/zxcv/删除）两端比普通字母宽（`side`）**，宽出来的那部分算在
// Shift / 删除自己的区间内——`bounds` 复位成 `sideVisibleFraction` 且分别居左 / 居右
// 显示，视觉上 Shift 紧贴外侧留白、删除紧贴另一侧留白，宽出来的部分变成 Shift 与 z、
// m 与删除之间的一道空隙。这道空隙从第三行自己的中缝 `bottomGap` 里扣
// （`bottomGap` = `gap` 少两倍的宽出量），第三行因此仍然自平。
//
// **第二行的中缝跟第三行对齐**（同用 `bottomGap`，不用 `gap`）：g 的右边缘与 v 的
// 右边缘对齐、两颗重复键（gSplit/vSplit）的左边缘也对齐。第三行比第一行窄下去的
// 那部分宽度，第二行不找 Shift / 删除要，改由 a、l 两颗边缘键各扛一半——它们跟
// Shift / 删除一样宽出 `side`（凑巧算出来的宽出量相等，两处直接共用同一个值），
// 可见区域仍是 `sideVisibleFraction` 那么大，但贴的是内侧：a 贴 s 那一侧（居右），
// l 贴 k 那一侧（居左），宽出来的部分落在 a 和外侧留白之间、l 和外侧留白之间，
// 而不是落在字母与字母之间——这样 a/l 多出来的触摸区依旧是「伸向屏幕边缘」，
// 跟合并态 `homeRowLeft`/`homeRowRight` 那个触摸区技巧的方向一致。
//
// 三套键盘（拼音 / 数字 / 符号）行的骨架完全一样——前两行十键（含中缝里那颗复制字母）、
// 第三行「侧键 + 七键（含复制字母） + 侧键」、第四行「切页 + 空格 + 中缝 + 空格 + 回车」——
// 这张表三边共用。`margin` 之前给到 25，太宽——手指还够得着的边缘留出一整条空白，
// 视觉上像是布局出错。缩到 8（约一条键缝的宽度，够跟中缝区分开，不再像 bug），
// 省下来的 34（两侧各 17）全部并进中缝 `gap` / `bottomGap`：分体本来就是要把两半键往
// 屏幕边缘推、中间空出大拇指去点的空间，缩边距、扩中缝正好是同一个方向。
//
//   第一行  8 + 5×79.2 + 317     + 5×79.2 + 8              = 1125
//   第二行  8 + 99 + 4×79.2 + 277.4 + 79.2 + 3×79.2 + 99 + 8 = 1125
//   第三行  8 + 99 + 4×79.2 + 277.4 + 79.2 + 3×79.2 + 99 + 8 = 1125
//   第四行  8 + 126 + 270 + 317 + 270 + 126 + 8              = 1125
local iw = {
  unit: '79.2/1125',  // 分体态的一颗普通键（字母 / 数字 / 符号），比合并态（112.5）窄一点
  margin: '8/1125',  // 四行共用的外侧留白
  gap: '317/1125',  // 第一 / 四行共用的中缝
  side: '99/1125',  // 第二行 a/l、第三行 Shift/删除的整颗触摸宽度，比 unit 宽 19.8
  sideVisibleFraction: '4/5',  // `side` 里视觉可见的比例，其余是键与相邻键之间的空隙
  bottomGap: '277.4/1125',  // 第二 / 三行共用的中缝，比 `gap` 窄（被两侧多出来的宽度吃掉）
  keyboardType: '126/1125',  // 第四行两端（切页 / 回车）
  space: '270/1125',  // 第四行空格，左右各一颗
};

// 分体态下把这颗键收成 0 宽——它在分体版面里没有位置。
// 合并态不受影响，这颗键照常显示。
local hidden = { split: { size: { width: 0 } } };

// 分体态下这颗键占多宽。
local width(value) = { split: { size: { width: value } } };

// 分体态下这颗键占多宽，**同时复位 `bounds`**。
//
// `bounds.width` 那个分数是相对**这颗键自身实际宽度**的比例
// （`KeyboardLayoutAlignment.getFrame` 拿实际渲染的 rect 去乘这个比例），不是绝对值。
// 「触摸区宽于显示区」那个技巧（`Widths.iPhone.homeRowLeft/homeRowRight/sideLeft/sideRight`）
// 就是靠这个比例，把显示宽度缩回普通键的视觉宽度。这个比例是按**合并态**的触摸宽度
// 标定的：分体把 `size` 改窄之后，同一个比例乘在更窄的实际宽度上，算出来的显示宽度
// 会比分体态的其它键更窄一截——边缘键会看着比邻居瘦一圈。
// 复位成 `1/1`（=100%）让显示宽度回到跟实际宽度相等，等于关掉这个技巧。
// 对没有 `bounds` 的键，这行是无操作——没有 `bounds` 时视觉区本就等于实际区。
local widthNoClip(value) = { split: { size: { width: value }, bounds: { width: '1/1' } } };

// 分体态下这颗键占多宽，且视觉区只占其中 `visibleFraction`、贴 `alignment` 那一侧显示。
// 跟 `widthNoClip` 反过来：那个是关掉「触摸宽于显示」的技巧，这个是主动打开它——
// 把整颗键的触摸区撑宽，可见的键帽仍是原来的大小、贴着外侧对齐，多出来的宽度变成
// 键帽和下一颗键之间的一道空隙（第三行 Shift / 删除两端的额外间距就是这么来的）。
local widthAnchored(value, visibleFraction, alignment) =
  { split: { size: { width: value }, bounds: { width: visibleFraction, alignment: alignment } } };

// 平时 0 宽、分体态撑开的实体键：中缝与两侧留白都是它。
//
// **必须是真实存在的样式节点**：取不到样式就不建图层，落在缝上的触摸会被吸附到
// 最近的真键，中缝两侧成片误触。也**必须没有 action**，点中缝安静落空。
local blankBackgroundName = 'splitBlankBackgroundStyle';
local spacer(name, splitWidth) = {
  [name]: {
    backgroundStyle: blankBackgroundName,
    size: { width: 0 },
    split: { size: { width: splitWidth } },
  },
};

{
  widths: w,
  iPhoneWidths: iw,
  hidden: hidden,
  width: width,
  widthNoClip: widthNoClip,
  widthAnchored: widthAnchored,
  spacer: spacer,
  blankBackgroundName: blankBackgroundName,

  // 进出分体的手势：挂在某颗键的上划上，挂在哪颗键由调用方决定
  // （iPad 挂在 Tab 上；iPhone 没有 Tab，挂在该页行首那颗分体后仍要用的功能键——
  // 拼音页是 Shift，数字页是 #+=，符号页是 123）。
  // 写在节点本层、不放进 `split` 覆盖块，所以合并态用它进分体，分体态用它再上划一次
  // 就合回去，两态共用同一个手势，不需要另开一颗「点一下合回去」的按钮。
  enterSplitGesture:: { swipeUpAction: { shortcut: '#toggleSplitState' } },

  // 分体版面要用到的共享样式节点
  shared:: {
    // 中缝与两侧留白的底：全透明（8 位色值是 RRGGBBAA），深浅色都一样，
    // 只为了让这些键有图层、能占住位置——落在缝上的触摸被它吸住，不会跑到隔壁真键上。
    [blankBackgroundName]: Style.geometry({ normalColor: '#00000000' }),
  },
}
